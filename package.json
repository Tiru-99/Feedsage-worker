{
  "name": "worker",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy --minify",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings"
  },
  "dependencies": {
    "@google/genai": "^1.34.0",
    "@types/bun": "^1.3.5",
    "hono": "^4.11.3"
  },
  "devDependencies": {
    "wrangler": "^4.4.0"
  }
}